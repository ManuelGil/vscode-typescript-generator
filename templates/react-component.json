{
	"name": "React Component",
	"description": "Template for generating a React functional component",
	"type": "component",
	"template": [
		"interface {{fileNamePascalCase}}Props {",
		"  children?: React.ReactNode;",
		"  title?: string;",
		"}",
		"",
		"/**",
		" * Renders the {{fileNamePascalCase}} component.",
		" */",
		"export function {{fileNamePascalCase}}({ children, title = '{{fileNamePascalCase}}' }: {{fileNamePascalCase}}Props) {",
		"  return (",
		"    <section className=\"{{fileNameKebabCase}}-container\">",
		"      <h1>{title}</h1>",
		"      {children}",
		"    </section>",
		"  );",
		"}"
	]
}
