{
	"name": "Fastify Route",
	"description": "Template for generating a Fastify route",
	"type": "route",
	"template": [
		"import { FastifyInstance } from 'fastify';",
		"import { {{fileNamePascalCase}} } from '../controllers/{{fileName}}.{{fileType}}';",
		"",
		"/**",
		" * Registers the {{fileName}} route.",
		" */",
		"export default async function register{{fileNamePascalCase}}Route(fastify: FastifyInstance): Promise<void> {",
		"  fastify.get('/{{fileName}}/health', async () => ({ status: 'ok' }));",
		"  fastify.post('/{{fileName}}', {{fileNamePascalCase}});",
		"}"
	]
}
