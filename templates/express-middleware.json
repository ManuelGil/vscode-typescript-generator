{
	"name": "Express Middleware",
	"description": "Template for generating an Express.js middleware",
	"type": "middleware",
	"template": [
		"import { Request, Response, NextFunction } from 'express';",
		"",
		"/**",
		" * Runs the {{fileNamePascalCase}} middleware.",
		" */",
		"export function {{fileNamePascalCase}}(request: Request, response: Response, next: NextFunction): void {",
		"  const requestId = request.header('x-request-id')?.trim() || `${Date.now()}-${Math.random().toString(16).slice(2)}`;",
		"  response.setHeader('x-request-id', requestId);",
		"  next();",
		"}"
	]
}
