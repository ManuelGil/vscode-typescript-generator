{
	"name": "Variable Template",
	"description": "Template for generating a TypeScript variable",
	"type": "variable",
	"template": [
		"export const {{fileNameCamelCase}}: Record<string, unknown> = {",
		"  isEnabled: true,",
		"  timeoutMs: 5000,",
		"  retries: 3",
		"};"
	]
}
