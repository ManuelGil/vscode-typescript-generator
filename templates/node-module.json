{
	"name": "Node.js Module",
	"description": "Template for generating a Node.js module",
	"type": "module",
	"template": [
		"interface {{fileNamePascalCase}}Options {",
		"  name?: string;",
		"  enabled?: boolean;",
		"}",
		"",
		"/**",
		" * Creates a module configuration object.",
		" */",
		"export function {{fileNameCamelCase}}(options: {{fileNamePascalCase}}Options = {}): { name: string; enabled: boolean } {",
		"  const name = options.name?.trim() || '{{fileNameCamelCase}}';",
		"",
		"  return {",
		"    name,",
		"    enabled: options.enabled ?? true",
		"  };",
		"}"
	]
}
